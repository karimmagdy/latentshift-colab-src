# HAT on Split-CIFAR-10
method: hat
benchmark: split_cifar10
benchmark_args:
  batch_size: 64
  classes_per_task: 2

encoder: hat_resnet18
latent_dim: 256
num_tasks: 10

# HAT-specific
s_max: 400.0
mask_reg_coeff: 0.01

# Training
epochs: 20
lr: 0.01
