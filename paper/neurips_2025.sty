% neurips_2025.sty — Style file for NeurIPS 2025
% Based on the official NeurIPS formatting requirements
\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{neurips_2025}[2025/01/01 NeurIPS 2025 submission style]

% Options
\newif\if@preprint\@preprintfalse
\newif\if@final\@finalfalse
\newif\if@nonatbib\@nonatbibfalse
\DeclareOption{preprint}{\@preprinttrue}
\DeclareOption{final}{\@finaltrue}
\DeclareOption{nonatbib}{\@nonatbibtrue}
\ProcessOptions\relax

% Font: Times
\RequirePackage{times}
\RequirePackage{latexsym}

% Page layout: 5.5in x 9in text area on letterpaper
\RequirePackage[letterpaper,
  textwidth=5.5in,
  textheight=9in,
  top=1in,
  left=1in,
  headheight=14pt]{geometry}

% Line spacing
\RequirePackage{setspace}
\setstretch{1.0}

% Section formatting
\RequirePackage{titlesec}
\titleformat{\section}{\large\bfseries}{\thesection}{1em}{}
\titleformat{\subsection}{\normalsize\bfseries}{\thesubsection}{1em}{}
\titleformat{\subsubsection}{\normalsize\itshape}{\thesubsubsection}{1em}{}
\titlespacing*{\section}{0pt}{2.0ex plus 1ex minus .2ex}{1.0ex plus .2ex}
\titlespacing*{\subsection}{0pt}{1.5ex plus 1ex minus .2ex}{0.8ex plus .2ex}

% Footnote rule
\renewcommand\footnoterule{%
  \kern-3\p@
  \hrule width 2in
  \kern 2.6\p@}

% Title formatting
\def\@maketitle{%
  \newpage
  \null
  \vskip 0.1in
  \begin{center}%
    {\LARGE\bfseries \@title \par}%
    \vskip 0.2in
    \if@final
      {\normalsize
       \begin{tabular}[t]{c}%
         \@author
       \end{tabular}\par}%
    \else
      {\normalsize Anonymous Author(s)\par}%
      {\small Submitted to NeurIPS 2025. Do not distribute.\par}%
    \fi
    \vskip 0.3in
  \end{center}}

% Abstract formatting
\renewenvironment{abstract}{%
  \centerline{\large\bfseries Abstract}%
  \vspace{0.5ex}%
  \begin{quote}\small}%
  {\end{quote}\vspace{1ex}}

% Footer
\RequirePackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
\if@final
  \fancyfoot[C]{\thepage}
\else
  \fancyfoot[C]{\small Submitted to NeurIPS 2025 \hfill \thepage}
\fi

% Natbib
\if@nonatbib\else
  \RequirePackage[round,sort&compress,numbers]{natbib}
  \bibliographystyle{plainnat}
\fi

% Hyperref
\RequirePackage[colorlinks,citecolor=blue,urlcolor=blue,linkcolor=blue]{hyperref}

\endinput
