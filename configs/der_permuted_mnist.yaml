# DER++ on Permuted-MNIST
method: der
benchmark: permuted_mnist
benchmark_args:
  batch_size: 64
  num_tasks: 10

encoder: mlp
input_dim: 784
hidden_dim: 400
latent_dim: 256

# DER++-specific
buffer_size_per_task: 500
replay_batch_size: 64
alpha: 1.0
beta: 1.0

# Training
epochs: 10
lr: 0.01
