# PackNet on Permuted-MNIST
method: packnet
benchmark: permuted_mnist
benchmark_args:
  batch_size: 64
  num_tasks: 10

encoder: mlp
input_dim: 784
hidden_dim: 400
latent_dim: 256

prune_ratio: 0.75
retrain_epochs: 0

epochs: 10
lr: 0.01
