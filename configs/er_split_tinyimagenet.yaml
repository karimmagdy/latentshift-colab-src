# Experience Replay on Split-TinyImageNet
method: er
benchmark: split_tinyimagenet
benchmark_args:
  batch_size: 64
  classes_per_task: 20

encoder: resnet18
small_input: false
latent_dim: 256

buffer_size_per_task: 200
replay_batch_size: 64

epochs: 20
lr: 0.01
