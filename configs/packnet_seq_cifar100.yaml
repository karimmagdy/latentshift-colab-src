# PackNet on Seq-CIFAR-100 (20 tasks × 5 classes)
method: packnet
benchmark: seq_cifar100
benchmark_args:
  batch_size: 64

encoder: resnet18
latent_dim: 256

# PackNet-specific
prune_ratio: 0.75
retrain_epochs: 0

# Training
epochs: 20
lr: 0.01
