# EWC on Seq-CIFAR-100 (20 tasks × 5 classes)
method: ewc
benchmark: seq_cifar100
benchmark_args:
  batch_size: 64

encoder: resnet18
latent_dim: 256

# EWC-specific
ewc_lambda: 400.0
num_fisher_samples: 200

# Training
epochs: 20
lr: 0.01
