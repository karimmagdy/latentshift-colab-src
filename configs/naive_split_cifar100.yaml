# Naive fine-tuning on Split-CIFAR-100
method: naive
benchmark: split_cifar100
benchmark_args:
  batch_size: 64
  classes_per_task: 10

encoder: resnet18
latent_dim: 256

# Training
epochs: 20
lr: 0.01
