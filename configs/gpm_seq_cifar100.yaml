# GPM on Seq-CIFAR-100 (20 tasks × 5 classes)
method: gpm
benchmark: seq_cifar100
benchmark_args:
  batch_size: 64

encoder: resnet18
latent_dim: 256
num_tasks: 50

# GPM-specific
threshold: 0.99
num_samples: 300

# Training
epochs: 20
lr: 0.01
