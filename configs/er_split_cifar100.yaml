# Experience Replay on Split-CIFAR-100
method: er
benchmark: split_cifar100
benchmark_args:
  batch_size: 64
  classes_per_task: 10

encoder: resnet18
latent_dim: 256

# ER-specific
buffer_size_per_task: 200
replay_batch_size: 64

# Training
epochs: 20
lr: 0.01
