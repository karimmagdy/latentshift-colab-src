# Naive fine-tuning baseline on Split-MNIST
method: naive
benchmark: split_mnist
benchmark_args:
  batch_size: 64
  classes_per_task: 2

encoder: mlp
input_dim: 784
hidden_dim: 400
latent_dim: 256

epochs: 10
lr: 0.01
