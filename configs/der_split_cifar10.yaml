# DER++ on Split-CIFAR-10
method: der
benchmark: split_cifar10
benchmark_args:
  batch_size: 64
  classes_per_task: 2

encoder: resnet18
latent_dim: 256

# DER++-specific
buffer_size_per_task: 500
replay_batch_size: 64
alpha: 1.0
beta: 1.0

# Training
epochs: 20
lr: 0.01
