# Naive (fine-tuning) with ViT-Tiny on Split-CIFAR-100
method: naive
benchmark: split_cifar100
benchmark_args:
  batch_size: 64
  classes_per_task: 10

encoder: vit_tiny
latent_dim: 256
img_size: 32
patch_size: 4
in_channels: 3

# Training
epochs: 20
lr: 0.001
