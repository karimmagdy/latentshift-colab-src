# Experience Replay on Permuted-MNIST
method: er
benchmark: permuted_mnist
benchmark_args:
  batch_size: 64
  num_tasks: 10

encoder: mlp
input_dim: 784
hidden_dim: 400
latent_dim: 256

# ER-specific
buffer_size_per_task: 200
replay_batch_size: 64

# Training
epochs: 10
lr: 0.01
